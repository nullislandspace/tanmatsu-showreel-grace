#pragma once
// Host stand-in for pax_gfx.h (make scenecheck): only the names the
// engine's scene headers mention. Nothing is drawn on the host.
#include <stdint.h>
typedef struct pax_buf pax_buf_t;
typedef uint32_t       pax_col_t;
